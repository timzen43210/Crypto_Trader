# 跟單群組真實訊號（pionex_strategy4.py 用來比對，屬於他人資料，不公開）
signals.csv

# 回測快取（本機重跑用，不需要進版控）
pionex_cache/
__pycache__/
*.pyc

# 實盤 / paper trading 的執行期資料（SQLite、日誌等）。
# 不可以放進 state/ 或 output/：dryrun.yml 與 run.sh 每小時 `git add state output`
# 之後自動 commit push，放那裡會把交易紀錄與日誌一路推進版控。
runtime/
